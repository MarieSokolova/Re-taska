{% extends 'base.html.twig' %}

{% block title %}Nový produkt{% endblock %}

{% block body %}
    <br>
    <div class="row">
<div class="col-md-6">
    <h1>Vytvořit nový produkt</h1>
    
    {{ form_start(form) }}
    {{ form_row(form.nazev) }}
{{ form_row(form.popis) }}
{{ form_row(form.cena) }}
{{ form_row(form.pocetKusu) }}


 <button class="btn">{{ button_label|default('Uložit') }}</button>
{{ form_end(form) }}
  
    <br>
    <a class="btn btn-primary" href="{{ path('product_index') }}" role="button">Zpět na výpis produktů</a>
     </div>
{% endblock %}
