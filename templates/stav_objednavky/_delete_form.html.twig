<form method="post" action="{{ path('stav_objednavky_delete', {'id': stav_objednavky.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stav_objednavky.id) }}">
    <button class="btn">Delete</button>
</form>
