{% extends 'base.html.twig' %}

{% block title %}Zeme{% endblock %}

{% block body %}
    <h1>Zeme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zeme.id }}</td>
            </tr>
            <tr>
                <th>ZemeDoruceni</th>
                <td>{{ zeme.zemeDoruceni }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('zeme_index') }}">back to list</a>

    <a href="{{ path('zeme_edit', {'id': zeme.id}) }}">edit</a>

    {{ include('zeme/_delete_form.html.twig') }}
{% endblock %}
