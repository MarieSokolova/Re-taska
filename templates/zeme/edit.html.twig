{% extends 'base.html.twig' %}

{% block title %}Edit Zeme{% endblock %}

{% block body %}
    <h1>Edit Zeme</h1>

    {{ include('zeme/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('zeme_index') }}">back to list</a>

    {{ include('zeme/_delete_form.html.twig') }}
{% endblock %}
